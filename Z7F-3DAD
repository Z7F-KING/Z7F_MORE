-- Services
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer

-- Protected Users
local ProtectedUsers = {
	["1il5f"] = true,
	["1il5i"] = true,
	["eyad51533"] = true,
	["1il5ff"] = true,
}

-- GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Z7F_GUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = game:GetService("CoreGui") -- هنا زر Z يظهر فورًا

-- Helper: Button Pop Effect
local function addPopEffect(button)
	local originalSize = button.Size
	
	button.MouseEnter:Connect(function()
		TweenService:Create(button,TweenInfo.new(0.12),{
			Size = originalSize + UDim2.new(0,2,0,2)
		}):Play()
	end)
	
	button.MouseLeave:Connect(function()
		TweenService:Create(button,TweenInfo.new(0.12),{
			Size = originalSize
		}):Play()
	end)
end

-- Main Frame
local Frame = Instance.new("Frame", ScreenGui)
Frame.Size = UDim2.new(0,300,0,325)
Frame.Position = UDim2.new(0.5,0,0.5,0)
Frame.AnchorPoint = Vector2.new(0.5,0.5)
Frame.BackgroundColor3 = Color3.fromRGB(0,0,0)
Frame.BackgroundTransparency = 0.25
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.Draggable = true
Instance.new("UICorner",Frame).CornerRadius = UDim.new(0,15)
Instance.new("UIStroke",Frame).Color = Color3.fromRGB(150,150,150)

-- Title
local Title = Instance.new("TextLabel",Frame)
Title.Size = UDim2.new(1,0,0,40)
Title.BackgroundTransparency = 1
Title.Text = "Z7F 3DAD"
Title.TextColor3 = Color3.new(1,1,1)
Title.Font = Enum.Font.GothamBold
Title.TextScaled = true

-- Drop Button
local DropButton = Instance.new("TextButton",Frame)
DropButton.Size = UDim2.new(0.9,0,0,40)
DropButton.Position = UDim2.new(0.05,0,0,50)
DropButton.BackgroundColor3 = Color3.fromRGB(30,30,30)
DropButton.Text = "اطـرح الـقـائـمـة"
DropButton.TextColor3 = Color3.new(1,1,1)
DropButton.Font = Enum.Font.Gotham
DropButton.TextScaled = true
DropButton.BorderSizePixel = 0
Instance.new("UICorner",DropButton).CornerRadius = UDim.new(0,10)
addPopEffect(DropButton)

-- Main Scroll
local MainScroll = Instance.new("ScrollingFrame",Frame)
MainScroll.Size = UDim2.new(0.9,0,0.6,0)
MainScroll.Position = UDim2.new(0.05,0,0,100)
MainScroll.BackgroundColor3 = Color3.fromRGB(25,25,25)
MainScroll.ScrollBarThickness = 6
MainScroll.BorderSizePixel = 0
Instance.new("UICorner",MainScroll).CornerRadius = UDim.new(0,10)

local Layout = Instance.new("UIListLayout",MainScroll)
Layout.Padding = UDim.new(0,5)

Layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	MainScroll.CanvasSize = UDim2.new(0,0,0,Layout.AbsoluteContentSize.Y+5)
end)

-- DropFrame
local DropFrame = Instance.new("ScrollingFrame",Frame)
DropFrame.Size = UDim2.new(0.9,0,0,200)
DropFrame.Position = UDim2.new(0.05,0,0,100)
DropFrame.BackgroundColor3 = Color3.fromRGB(25,25,25)
DropFrame.ScrollBarThickness = 6
DropFrame.BorderSizePixel = 0
DropFrame.Visible = false
Instance.new("UICorner",DropFrame).CornerRadius = UDim.new(0,10)

local dropOpen = false
DropButton.MouseButton1Click:Connect(function()
	dropOpen = not dropOpen
	DropFrame.Visible = dropOpen
end)

local addedPlayers = {}

-- Update Drop List
local function updateDropList()
	DropFrame:ClearAllChildren()
	local y = 0
	
	for _,plr in pairs(Players:GetPlayers()) do
		if ProtectedUsers[plr.Name] then continue end
		
		local item = Instance.new("TextButton",DropFrame)
		item.Size = UDim2.new(1,0,0,50)
		item.Position = UDim2.new(0,0,0,y)
		item.BackgroundColor3 = Color3.fromRGB(40,40,40)
		item.BorderSizePixel = 0
		item.Text = ""
		Instance.new("UICorner",item).CornerRadius = UDim.new(0,8)

		local avatar = Instance.new("ImageLabel",item)
		avatar.Size = UDim2.new(0,40,0,40)
		avatar.Position = UDim2.new(0,5,0,5)
		avatar.BackgroundTransparency = 1
		avatar.Image = Players:GetUserThumbnailAsync(plr.UserId,Enum.ThumbnailType.HeadShot,Enum.ThumbnailSize.Size48x48)
		Instance.new("UICorner",avatar).CornerRadius = UDim.new(1,0)

		local name = Instance.new("TextLabel",item)
		name.Size = UDim2.new(0.6,0,0,20)
		name.Position = UDim2.new(0,50,0,5)
		name.BackgroundTransparency = 1
		name.Text = plr.Name
		name.TextColor3 = Color3.new(1,1,1)
		name.Font = Enum.Font.GothamBold
		name.TextScaled = true
		name.TextXAlignment = Enum.TextXAlignment.Left

		local id = Instance.new("TextLabel",item)
		id.Size = UDim2.new(0.6,0,0,15)
		id.Position = UDim2.new(0,50,0,25)
		id.BackgroundTransparency = 1
		id.Text = "ID: "..plr.UserId
		id.TextColor3 = Color3.fromRGB(200,200,200)
		id.Font = Enum.Font.Gotham
		id.TextScaled = true
		id.TextXAlignment = Enum.TextXAlignment.Left

		item.MouseButton1Click:Connect(function()
			if addedPlayers[plr.UserId] then return end
			
			local f = Instance.new("Frame",MainScroll)
			f.Size = UDim2.new(1,-5,0,50)
			f.BackgroundColor3 = Color3.fromRGB(40,40,40)
			f.BackgroundTransparency = 1
			Instance.new("UICorner",f).CornerRadius = UDim.new(0,8)

			local av = avatar:Clone()
			av.Parent = f

			local nm = name:Clone()
			nm.Parent = f
			nm.Position = UDim2.new(0,50,0,5)

			local stats = Instance.new("TextLabel",f)
			stats.Size = UDim2.new(0.6,0,0,15)
			stats.Position = UDim2.new(0,50,0,25)
			stats.BackgroundTransparency = 1
			stats.Text = "دخول 0 | خروج 0"
			stats.TextColor3 = Color3.fromRGB(200,200,200)
			stats.Font = Enum.Font.Gotham
			stats.TextScaled = true
			stats.TextXAlignment = Enum.TextXAlignment.Left

			-- Copy Button (يسار X)
			local copyBtn = Instance.new("ImageButton",f)
			copyBtn.Size = UDim2.new(0,25,0,25)
			copyBtn.Position = UDim2.new(1,-60,0,12)
			copyBtn.BackgroundColor3 = Color3.fromRGB(120,120,120)
			copyBtn.Image = "rbxassetid://90434151822042"
			Instance.new("UICorner",copyBtn).CornerRadius = UDim.new(0,5)
			addPopEffect(copyBtn)

			copyBtn.MouseButton1Click:Connect(function()
				local data = addedPlayers[plr.UserId]
				if data then
					local text = plr.Name.." | دخول "..data.Join.." | خروج "..data.Leave
					if setclipboard then
						setclipboard(text)
					end
				end
			end)

			-- Close Button
			local close = Instance.new("TextButton",f)
			close.Size = UDim2.new(0,25,0,25)
			close.Position = UDim2.new(1,-30,0,12)
			close.BackgroundColor3 = Color3.fromRGB(200,0,0)
			close.Text = "X"
			close.TextColor3 = Color3.new(1,1,1)
			close.Font = Enum.Font.GothamBold
			close.TextScaled = true
			Instance.new("UICorner",close).CornerRadius = UDim.new(0,5)
			addPopEffect(close)

			TweenService:Create(f,TweenInfo.new(0.25),{BackgroundTransparency=0}):Play()

			addedPlayers[plr.UserId] = {Stats=stats,Leave=0,Join=0}

			close.MouseButton1Click:Connect(function()
				local tween = TweenService:Create(f,TweenInfo.new(0.25),{BackgroundTransparency=1})
				tween:Play()
				tween.Completed:Connect(function()
					f:Destroy()
					addedPlayers[plr.UserId] = nil
				end)
			end)
		end)

		y += 55
	end
	
	DropFrame.CanvasSize = UDim2.new(0,0,0,y)
end

Players.PlayerAdded:Connect(function(plr)
	updateDropList()
	local d = addedPlayers[plr.UserId]
	if d then
		d.Join += 1
		d.Stats.Text = "دخول "..d.Join.." | خروج "..d.Leave
	end
end)

Players.PlayerRemoving:Connect(function(plr)
	updateDropList()
	local d = addedPlayers[plr.UserId]
	if d then
		d.Leave += 1
		d.Stats.Text = "دخول "..d.Join.." | خروج "..d.Leave
	end
end)

updateDropList()

-- Toggle Z Button (يظهر فورًا)
local ToggleBtn = Instance.new("TextButton",ScreenGui)
ToggleBtn.Size = UDim2.new(0,50,0,50)
ToggleBtn.Position = UDim2.new(1,-60,0.3,-25)
ToggleBtn.AnchorPoint = Vector2.new(0,0.5)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0,0,0)
ToggleBtn.BackgroundTransparency = 0.25
ToggleBtn.Text = "Z"
ToggleBtn.TextColor3 = Color3.new(1,1,1)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextScaled = true
ToggleBtn.BorderSizePixel = 0
Instance.new("UICorner",ToggleBtn).CornerRadius = UDim.new(1,0)
local ZStroke = Instance.new("UIStroke",ToggleBtn)
ZStroke.Color = Color3.fromRGB(150,150,150)
ZStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

ToggleBtn.MouseButton1Click:Connect(function()
	Frame.Visible = not Frame.Visible
end)
